// Math

let resultado;

// PI
//resultado = Math.PI;

// redondear
resultado = Math.round(2.1);
resultado = Math.round(2.9);
resultado = Math.round(2.4);
resultado = Math.round(2.6);
resultado = Math.round(2.5);

// redondear hacia abajo
resultado = Math.floor(2.9);
resultado = Math.floor(2.1);

// redondear hacia arriba
resultado = Math.ceil(2.9);
resultado = Math.ceil(2.1);

// raiz cuadrada
resultado = Math.sqrt(144);

// potencia
resultado = Math.pow(2, 5);

// minimo
resultado = Math.min(1,9,2,-4,10)

// maximo
resultado = Math.max(1,9,2,-4,10)

// aleatorio (de cero a 0.99)
resultado = Math.random();

// aleatorio del 0 al 10, lo multiplico por el y lo redondeo
resultado = Math.round(Math.random() * 10);


console.log(resultado);