// objetos
const nombre = 'televisor';
const precio = 300;
const disponible = true;

// construccion de un objeto literal, se pueden agrupar todas las propiedades en una sola variable

const producto = {
    nombre: 'televisor',
    precio: 300,
    disponible: true
}

console.log(producto);