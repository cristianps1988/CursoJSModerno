// acceder a las propiedades de un objeto

const producto = {
    nombre: 'televisor',
    precio: 300,
    disponible: true
}

console.log(producto);

// sintaxis de punto. Es la mas comun
console.log(producto.nombre);

// sintaxis de corchete
console.log(producto['nombre']);